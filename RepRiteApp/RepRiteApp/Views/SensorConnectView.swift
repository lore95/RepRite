// MARK: - Sensor Connect View
struct SensorConnectView: View {
    var body: some View {
        VStack {
            Text("Sensor Connection")
                .font(.largeTitle)
            // Add sensor connection logic here
            Spacer()
        }
        .padding()
    }
}